[project]
name = "crm"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "django>=6.1",
    "django-htmx>=1.29.0",
]

[dependency-groups]
dev = [
    "playwright>=1.62.0",
]
